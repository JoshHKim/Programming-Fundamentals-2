#include <iostream>
using namespace std;
int main(){
    for(unsigned int i=0; i<=10; i++){
        string option;
        cin >> option;
        int integer=stoi(option);
        cout<< (integer^7);
    }

}